apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce
  labels:
    release: monitoring
    app: order-service

spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP