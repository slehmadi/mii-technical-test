apiVersion: v1
kind: Service
metadata:
  name: product-db
  namespace: ecommerce

spec:
  clusterIP: None
  selector:
    app: product-db
  ports:
    - name: http
      port: 3306
      targetPort: 3306