apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: ecommerce

spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  
  template:
    metadata:
      labels:
        app: auth-service
    
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/slehmadi/auth-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DB_HOST
              value: auth-db
            
            - name: DB_PORT
              value: "3306"

            - name: DB_NAME
              value: ecommerce_auth
            
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: auth-service-secret
                  key: DB_USER
            
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-service-secret
                  key: DB_PASSWORD
            
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: auth-service-secret
                  key: SECRET_KEY
            
            - name: ALGORITHM
              valueFrom:
                secretKeyRef:
                  name: auth-service-secret
                  key: ALGORITHM
            
            - name: ACCESS_TOKEN_EXPIRE_MINUTES
              valueFrom:
                secretKeyRef:
                  name: auth-service-secret
                  key: ACCESS_TOKEN_EXPIRE_MINUTES
            
            - name: BACKEND_CORS_ORIGINS
              value: '["http://shop.local"]'